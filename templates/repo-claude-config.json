{
  "$schema": "https://claude.ai/schemas/project-config.json",
  "version": "1.0.0",
  "extends": "../.claude/base-project.json",
  "description": "Base template for new raibid-labs repositories - customize for your project",

  "project": {
    "name": "{{PROJECT_NAME}}",
    "type": "{{PROJECT_TYPE}}",
    "description": "{{PROJECT_DESCRIPTION}}",
    "repository": "https://github.com/raibid-labs/{{REPO_NAME}}"
  },

  "language": {
    "primary": "{{PRIMARY_LANGUAGE}}",
    "secondary": []
  },

  "buildCommands": {
    "build": "{{BUILD_COMMAND}}",
    "test": "{{TEST_COMMAND}}",
    "lint": "{{LINT_COMMAND}}",
    "format": "{{FORMAT_COMMAND}}",
    "dev": "{{DEV_COMMAND}}"
  },

  "dependencies": {
    "external": [],
    "internal": []
  },

  "contextFiles": [
    "README.md",
    "CONTRIBUTING.md",
    "ARCHITECTURE.md"
  ],

  "rules": {
    "project": [
      "Follow project-specific coding standards defined in CONTRIBUTING.md",
      "Update ARCHITECTURE.md when making significant structural changes",
      "Document API changes in relevant documentation"
    ]
  },

  "customization": {
    "mcpServers": {},
    "workflows": {},
    "agents": []
  },

  "__instructions__": {
    "usage": "Replace all {{PLACEHOLDER}} values with project-specific information",
    "extends": "This config inherits from base-project.json - only override what's needed",
    "customization": "Add project-specific MCP servers, workflows, and agents in the customization section"
  }
}
