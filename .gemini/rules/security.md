# Security Rules

- Sanitize all user input to prevent injection attacks.
- Use parameterized queries to interact with databases.
- Do not store secrets in the codebase. Use a secure secret management solution.
